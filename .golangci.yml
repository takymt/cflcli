version: "2"

formatters:
  enable:
    - goimports

linters:
  enable:
    # Bug prevention
    - bodyclose
    - durationcheck
    - errcheck
    - errorlint
    - govet
    - nilerr
    - staticcheck

    # Code quality
    - gocritic
    - ineffassign
    - revive
    - unconvert
    - unused

    # Style
    - errname
    - whitespace

  settings:
    gocritic:
      enabled-tags:
        - diagnostic
        - performance
    revive:
      rules:
        - name: exported
